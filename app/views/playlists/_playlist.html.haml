%h2.kicker Playlist
.playlist
  - @items.each do |item|
    .playlist-item{ class: "playlist-item--#{item.class.name.downcase}",
                    data: { at: item.at.to_json }}
      = render item
