<!DOCTYPE html>
<html>
<head>
  <title>Readback <%= content_for?(:title) ? " — " + content_for(:title): "" %></title>
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
</head>
<body>

<header id="nav">
  <div class="left">
    <span><%= link_to "Playlist", root_path(anchor: 'now') %></span>
    <span><%= link_to "Schedule", Semester.current %></span>
  </div>
  <div class="right">
    <% if current_dj %>
      <%= link_to current_dj.name, current_dj %> &mdash;
      <%= link_to "Log out", destroy_dj_session_path, method: :delete %>
    <% else %>
      <%= link_to "Log in", new_dj_session_path %>
    <% end %>
  </div>
</header>

<%= yield %>

</body>
</html>
