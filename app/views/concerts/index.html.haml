- title "Concert"

%h3 Upcoming Concerts:
%table.events-list
  - @events.days.each do |date, events|
    %div.event-day
      %div.event-date
        %h1= l date, format: :long
        %hr
      %ul.events
      - events.each do |event|
        %li.event
          %h3= event.title
          %p= l event.beginning, format: :long
          %p= event.description


%p
  Concert info is brought to you by the locally owned Ann Arbor Observer,
  providing news and events online at annarborobserver.com and in print. Stay
  tuned for concert and events info, every other hour, on WCBN FM Ann Arbor.

